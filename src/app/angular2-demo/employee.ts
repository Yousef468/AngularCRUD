export class IEmployee {
    code: string;
    name: string;
    gender: string;
    salary: string;
    dateOfBirth: string;
}

export class Employee implements IEmployee {

    code: string;
    name: string;
    gender: string;
    salary: string;
    dateOfBirth: string;
}
